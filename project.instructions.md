---
name: 项目协作说明
description: 该文件定义了本项目的协作规范、代码风格、依赖管理和文案规范。所有任务都应遵循这些约定，以确保项目的一致性和可维护性。
---

# 项目协作说明

## 硬限制

- 严禁读取 `src/local_config.py` 文件。

## 协作原则

- 在现有项目结构内做最小必要修改，不要顺手重构无关模块。
- 优先复用现有代码、配置、枚举、存储封装和工具函数。
- 优先选择直接、清晰、可维护的实现，不做过度抽象。
- 保持现有目录结构、模块边界和公共接口稳定。
- 变量、函数、类名尽量使用简洁且语义明确的命名，遵循现有命名风格。

## 代码规范

- Python 版本要求为 3.11 及以上。
- 遵循 ruff 代码格式化规则，行宽为 120。
- 清理未使用的 import、变量、参数和失效辅助逻辑。

## 依赖管理

- 项目使用 uv 管理依赖。
- 若任务需要引入新的包，优先使用 uv 的规范方式添加依赖；因环境限制无法执行时，再直接修改 pyproject.toml。

## 注释与文案

- 默认主语言使用中文。
- 为关键逻辑和可能造成理解困难的代码添加充分的注释，解释为什么而不是复述代码做了什么。
- 中文与英文、数字混排时，应按「盘古之白」规范保留半角空格，应规范使用全角或半角标点。

## 交付要求

- 应确认改动范围应只覆盖当前任务的必要文件。
- 应自查是否存在无关修改、重复配置或重复逻辑。
- 若任务引入了新依赖，应确认依赖声明、代码引用和相关文档修改已经同步完成。
